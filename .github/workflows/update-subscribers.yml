name: Update subscribers count

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  update-subscribers:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install
        working-directory: Frontend

      - name: Ensure subscriber data directory exists
        run: mkdir -p Frontend/data

      - name: Update subscribers count
        run: node ../scripts/update-subscribers.js
        working-directory: Frontend
        env:
          YOUTUBE_CHANNEL_URL: ${{ secrets.YOUTUBE_CHANNEL_URL }}
          YOUTUBE_CHANNEL_ID: ${{ secrets.YOUTUBE_CHANNEL_ID }}

      - name: Commit updated subscribers data
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git add Frontend/data/subscribers.json
          git diff --quiet --cached || git commit -m 'chore: update subscribers count'

      - name: Push commit
        run: git push